<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{% if title %}{{ title }} • {% endif %}{{ site.name }}</title>
    <meta name="description" content="{{ description | truncate(140) if description else content | striptags | truncate(140) }}">


    <link rel="stylesheet" href="/assets/main.css?<%- cacheBust.mainCss %>" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,500;1,400&family=Inter:wght@400;700;900&display=swap" rel="stylesheet">

    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    <link rel="icon" type="image/png" href="/images/favicon.png" />

    {% include "social-meta.njk" %}

  </head>

  <body>
  <body class="type-{{ layout | lower }} page-{{ page.fileSlug if page.fileSlug != '_pages' else 'home' }}">
    <a class="sr-only" href="#main">Skip to content</a>

    {% include "header.njk" %}

    <main id="main">
      {{ content | safe }}
    </main>

    {# {% include "footer.njk" %} #}

    <script src="/assets/main.js?<%- cacheBust.mainJs %>"></script>
  </body>
</html>
